extends Node


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	print("test")
	#for i in range(10):
		#var vec = Vector2i(i, i)
		#print($TileMapLayer.get_cell_source_id(vec))
		#$TileMapLayer.set_cell(Vector2i(i, i))
		#$TileMapLayer.update_internals()
		#$TileMapLayer.update_configuration_warnings()
		#print($TileMapLayer.get_cell_source_id(vec))


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
